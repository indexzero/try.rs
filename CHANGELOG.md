## [unreleased]

### 🚀 Features

- M0 skeleton — workspace, CI gates, adopted conformance suite
- *(core)* M1 script core — argv, dispatch, emit, wrappers, usage spec
- *(core)* M2 selector + fuzzy + TUI — full conformance (387/387)
- M3 release engineering — dist pipeline, usage-spec artifacts, tooling
